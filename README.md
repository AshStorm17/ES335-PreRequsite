# ES335-PreRequsite
Run the below installations on yout local machine before running the notebook
```
pip install numpy
pip install matplotlib
pip install pandas
pip install jax
pip install sympy
```
